import type {PlanEntry} from './plan';
import type {VisualGuide} from './guides';
import type {Tool,RevealStep} from '../model';
const f=(text:string,tool:Tool,math?:string):RevealStep=>({text,tool,math});
const strip=(denominators:number[],shaded:number[]):Tool=>({kind:'fractions',denominators,shaded});
const picture=(picture:Extract<Tool,{kind:'diagram'}>['picture'],caption:string):Tool=>({kind:'diagram',picture,caption});
const bar=(whole:number|null,parts:(number|null)[],labels?:string[]):Tool=>({kind:'bar',whole,parts,labels});
/** Focused follow-on demonstrations for objectives that go beyond the family's first idea. */
export function focusedGuide(p:PlanEntry):VisualGuide|null{
 const c=p.code,o=p.objective.toLowerCase();
 if(c==='AS'&&/mental/.test(o)&&!/two-digit/.test(o)){const start=p.level===1?27:247,step=p.level===1?10:100;return {title:'Change one place, keep the others',frames:[f(`Start with ${start}. We want to add ${step}.`,{kind:'scene',scene:'place',values:[start,0,0],phase:0}),f(`Add one more ${step===10?'ten':'hundred'}. The other columns stay just as they were.`,{kind:'scene',scene:'place',values:[start+step,0,0],phase:1}),f('No exchange was needed. Take that same group away to return to the starting number.',{kind:'scene',scene:'place',values:[start+step,0,0],phase:2},`${start} + ${step} = ${start+step}`)]};}
 if(c==='MD'&&/multiply.*digits/.test(o))return {title:'A large group can be split into smaller groups',frames:[f('We have 4 rows of 23. Each row can be split into 20 and 3.',{kind:'scene',scene:'area',values:[23,4],phase:0},'23 × 4'),f('Four rows of 20 make 80. We can count those as four groups of two tens.',bar(80,[20,20,20,20]),'20 × 4 = 80'),f('The four extra groups of 3 make 12.',{kind:'array',rows:4,cols:3},'3 × 4 = 12'),f('Join both parts. Splitting the rows did not change the number of objects.',bar(92,[80,12]),'23 × 4 = 80 + 12 = 92')]};
 if(c==='MD'&&/divide.*digits/.test(o))return {title:'Share a useful part first',frames:[f('Share 84 counters equally between 4 groups. Split 84 into 80 and 4.',bar(84,[80,4]),'84 ÷ 4'),f('Share the 80 first. Each group receives 20.',{kind:'ratio',names:['Groups'],units:[4],unitValue:20,total:80},'80 ÷ 4 = 20'),f('The last 4 give each group one more counter.',{kind:'groups',groups:4,size:1},'4 ÷ 4 = 1'),f('Each group has 20 and 1, which is 21.',{kind:'ratio',names:['Groups'],units:[4],unitValue:21,total:84},'84 ÷ 4 = 21')]};
 if(c==='AS'&&/three-digit|four-digit|three digits|four digits/.test(o)&&!/mentally/.test(o))return {title:'When the ones make another ten',frames:[f('Let’s see what happens when we add 37 and 15. The blue blocks show 37; orange shows 15.',{kind:'scene',scene:'regroup',values:[37,15],phase:0,labels:['Three blue tens and seven blue ones; one orange ten and five orange ones.']}),f('Join the ones. Seven and five make twelve ones. Ten of those ones can become a new ten.',{kind:'scene',scene:'regroup',values:[37,15],phase:1,labels:['Ten of the twelve ones are circled to form a new ten.']}),f('Exchange those ten ones for one ten. We have five tens and two ones: 52.',{kind:'scene',scene:'regroup',values:[37,15],phase:2,labels:['Five tens and two ones.']},'37 + 15 = 52'),f('The same exchange works in every column: ten tens make one hundred; ten hundreds make one thousand.',{kind:'scene',scene:'place',values:[520,0,0],phase:2},'370 + 150 = 520')]};
 if(c==='AREA'&&/composite/.test(o)&&/triangle/.test(o))return {title:'A rectangle with a triangular roof',frames:[f('Split the house shape at the dotted line. We have a rectangle and a triangle.',{kind:'scene',scene:'house',values:[6,4,2],phase:0,labels:['Rectangle six centimetres wide and four high; roof has perpendicular height two.']}),f('The rectangle covers 6 × 4 = 24 square centimetres. The roof covers half of 6 × 2, which is 6.',{kind:'scene',scene:'house',values:[6,4,2],phase:1,labels:['The triangular roof is highlighted.']}),f('The two parts meet along an edge, without overlapping. Add their areas.',{kind:'scene',scene:'house',values:[6,4,2],phase:2},'24 + 6 = 30 cm²')]};
 if(c==='CIRCLE'&&!/semicircle|quarter|composite/.test(o))return {title:'Why does circle area use the radius twice?',frames:[f('Cut a circle into equal sectors, like pizza slices. None of its area is lost.',{kind:'scene',scene:'circle-area',values:[7],phase:0}),f('Alternate the slices pointing up and down. They begin to look like a rectangle.',{kind:'scene',scene:'circle-area',values:[7],phase:1}),f('Its height approaches one radius. Its width approaches half the circumference, π times the radius.',{kind:'scene',scene:'circle-area',values:[7],phase:2}),f('Finer slices make the fit closer. Width times height gives πr × r.',{kind:'scene',scene:'circle-area',values:[7],phase:2},'circle area = πr²')]};
 if(c==='CIRCLE'&&/semicircle|quarter/.test(o))return {title:'The curved edge is not the whole perimeter',frames:[f('A semicircle contains half of the full circle’s area.',picture({type:'shape',shape:'semicircle',labels:['radius 7 cm']},'A semicircle of radius seven centimetres.'),'area = π × 7² ÷ 2'),f('Its curved edge is half the full circumference: 22 cm when π = 22/7.',picture({type:'shape',shape:'semicircle',labels:['curved edge 22 cm']},'Trace the curved edge.'),'arc = 2 × (22/7) × 7 ÷ 2 = 22 cm'),f('To walk all the way around, you must also walk the straight diameter, 14 cm.',picture({type:'shape',shape:'semicircle',labels:['diameter 14 cm']},'The straight edge belongs to the outer boundary too.'),'perimeter = 22 + 14 = 36 cm'),f('For a quarter-circle, use a quarter of the arc and add its two radii.',picture({type:'shape',shape:'quarter-circle',labels:['radius 7 cm']},'The boundary includes two radii and one quarter of the circle.'),'perimeter = 11 + 7 + 7 = 25 cm')]};
 if(c==='SHAPE'&&p.level>=5&&/triangle|unknown|composite/.test(o))return {title:'Make the known corners work for you',frames:[f('Two corners of this triangle are 50° and 60°. What turn is left for the third corner?',{kind:'scene',scene:'triangle-angles',values:[50,60],phase:0}),f('The three inside angles of a triangle add to 180°. The two known corners use 110°.',{kind:'scene',scene:'triangle-angles',values:[50,60],phase:1},'50° + 60° = 110°'),f('The remaining corner is 70°. Check by adding all three.',{kind:'scene',scene:'triangle-angles',values:[50,60],phase:2},'180° − 110° = 70°')]};
 if(c==='AS'&&/three one-digit/.test(o))return {title:'Choose a useful pair first',frames:[f('We want to join 6, 3 and 4 counters. Which two groups could make a ten?',{kind:'foundation-visual',visual:{type:'counters',groups:[{count:6,color:'blue',label:'6'},{count:3,color:'green',label:'3'},{count:4,color:'orange',label:'4'}]}}),f('Join 6 and 4 first. That makes a full ten-frame.',{kind:'counters',count:10,frame:10},'6 + 4 = 10'),f('The other 3 have not disappeared. Add them to our ten.',{kind:'counters',count:13,frame:20},'6 + 3 + 4 = 13')]};
 if(c==='AS'&&/connect|number bonds/.test(o))return {title:'One whole, two parts',frames:[f('Eight counters make one whole. Five are blue and three are orange.',{kind:'foundation-visual',visual:{type:'counters',groups:[{count:5,color:'blue'},{count:3,color:'orange'}]}}),f('A number bond keeps the same information: the whole is 8, and its parts are 5 and 3.',{kind:'bond',whole:8,parts:[5,3]},'5 + 3 = 8'),f('Cover the part with 3. Starting from 8 and taking away 5 leaves that other part.',{kind:'bond',whole:8,parts:[5,3],hide:'b'},'8 − 5 = 3')]};
 if(c==='AS'&&/symbol|correctly/.test(o))return {title:'The equals sign says “the same amount”',frames:[f('The left side has 3 and 2 counters. The right side has 5.',bar(5,[3,2]),'3 + 2 = 5'),f('Both sides describe five. The equals sign connects two names for the same amount.',{kind:'balance',left:{x:0,n:5},right:{x:0,n:5},xValue:1}),f('We can write the sides the other way round. They still name the same amount.',bar(5,[3,2]),'5 = 3 + 2')]};
 if(c==='WN'&&/multiply and divide/.test(o)||c==='MD'&&/10 100|powers/.test(o))return {title:'Ten times as much',frames:[f('Three tens and two ones make 32.',{kind:'scene',scene:'place',values:[32,0,0],phase:0}),f('Make every unit ten times as large. Three tens become three hundreds; two ones become two tens.',{kind:'scene',scene:'place',values:[320,0,0],phase:1}),f('The result is 320. Divide by ten to return each unit to its previous size.',{kind:'scene',scene:'place',values:[320,0,0],phase:2},'32 × 10 = 320; 320 ÷ 10 = 32')]};
 if(c==='FRAC'&&/multiply mixed/.test(o))return {title:'Multiply the wholes and the extra parts',frames:[f('Each bottle holds one and a half litres. We have three bottles.',strip([2,2,2,2,2,2],[2,1,2,1,2,1])),f('The three whole litres make 3 litres. The three extra halves make one and a half litres.',strip([2,2,2,2,2],[2,2,2,2,1]),'3 × 1 + 3 × 1/2'),f('Join those amounts: four and a half litres altogether.',strip([2,2,2,2,2],[2,2,2,2,1]),'1 1/2 × 3 = 4 1/2')]};
 if(c==='FRAC'&&/add|subtract/.test(o)&&/mixed/.test(o))return {title:'Whole pieces and fractional pieces',frames:[f('Start with two and one quarter. We want to take away three quarters.',strip([4,4,4],[4,4,1]),'2 1/4 − 3/4'),f('Exchange one whole for four quarters. The amount is still the same: one whole and five quarters.',strip([4,4,4],[4,4,1]),'2 1/4 = 1 + 5/4'),f('Remove three quarters from the five quarters. One whole and two quarters remain.',strip([4,4],[4,2]),'2 1/4 − 3/4 = 1 1/2')]};
 if(c==='FRAC'&&/like fractions/.test(o)&&!/unlike|unit/.test(o))return {title:'Same-sized pieces are easy to compare',frames:[f('Both equal-sized strips are cut into fifths.',strip([5,5],[0,0])),f('Shade three pieces on the first and two on the second.',strip([5,5],[3,2])),f('Every piece is the same size. Three pieces cover more than two.',strip([5,5],[3,2]),'3/5 > 2/5')]};
 if(c==='DEC'&&/compare|order/.test(o))return {title:'More digits need not mean more',frames:[f('Shade 4 tenths of the first whole.',{kind:'hundred',shaded:40},'0.4'),f('Now shade 35 hundredths of another equal whole. Its coloured area is smaller.',{kind:'hundred',shaded:35},'0.35'),f('Four tenths are 40 hundredths. Compare 40 small squares with 35 small squares.',{kind:'bar',whole:null,parts:[],compare:{top:.4,bottom:.35,names:['0.40','0.35']}},'0.40 > 0.35')]};
 if(c==='DEC'&&/add|subtract/.test(o))return {title:'Join pieces of the same size',frames:[f('Twenty-five hundredths means 25 little squares.',{kind:'hundred',shaded:25},'0.25'),f('Add 30 hundredths first. There are now 55 shaded squares.',{kind:'hundred',shaded:55},'0.25 + 0.30 = 0.55'),f('Add the remaining 8 hundredths. The whole has 63 shaded squares.',{kind:'hundred',shaded:63},'0.25 + 0.38 = 0.63'),f('Taking away those same 38 squares brings us back to 25.',{kind:'hundred',shaded:25},'0.63 − 0.38 = 0.25')]};
 if(c==='DEC'&&/convert measurement/.test(o))return {title:'Same length, smaller units',frames:[f('One metre is 100 centimetres. Half a metre is half of 100 centimetres.',bar(100,[50,50],['half a metre','half a metre'])),f('1.5 metres contains one full metre and another half metre.',bar(150,[100,50],['1 metre','0.5 metre'])),f('Count everything in centimetres: 100 and 50 make 150.',bar(150,[100,50],['100 cm','50 cm']),'1.5 m = 150 cm')]};
 if(c==='RATIO'&&/fraction/.test(o))return {title:'Which whole does the fraction mean?',frames:[f('Red to blue is 2 to 3. Red has two units; blue has three.',{kind:'ratio',names:['Red','Blue'],units:[2,3],unitValue:1,total:5}),f('For the fraction that is red, the whole includes both colours: five units.',strip([5],[2]),'2 + 3 = 5 units'),f('Two of all five units are red. Red is two fifths of the total.',strip([5],[2]),'red : blue = 2 : 3; red / total = 2/5')]};
 if(c==='ALG'&&/form|unknown|expression involving/.test(o)&&!/substitut|simplif/.test(o))return {title:'Use a letter for a hidden amount',frames:[f('Each bag has the same hidden number of counters. Call that number x.',{kind:'balance',left:{x:1,n:0},right:{x:1,n:0},xValue:4}),f('Three bags contain x + x + x counters. We write that more briefly as 3x.',{kind:'balance',left:{x:3,n:0},right:{x:3,n:0},xValue:4},'x + x + x = 3x'),f('Add two loose counters. The total is 3x + 2, whatever number is hidden in each bag.',{kind:'balance',left:{x:3,n:2},right:{x:3,n:2},xValue:4},'3x + 2')]};
 if(c==='ALG'&&/simplif/.test(o))return {title:'Count the bags, not what is inside',frames:[f('Two bags and three more identical bags contain five bags’ worth of counters.',{kind:'balance',left:{x:2,n:0},right:{x:2,n:0},xValue:3},'2x + 3x'),f('Join the bag counts. We have not opened a bag or changed what x means.',{kind:'balance',left:{x:5,n:0},right:{x:5,n:0},xValue:3},'2x + 3x = 5x'),f('Five bags still means five copies of x. It does not mean x × x.',{kind:'balance',left:{x:5,n:0},right:{x:5,n:0},xValue:3},'5x = x + x + x + x + x')]};
 if(c==='ALG'&&/substitut/.test(o))return {title:'Open every bag',frames:[f('The expression 3x + 2 means three equal bags and two loose counters.',{kind:'balance',left:{x:3,n:2},right:{x:3,n:2},xValue:4},'3x + 2'),f('We learn that each bag contains 4. Replace every x with 4.',{kind:'groups',groups:3,size:4},'3 × 4 + 2'),f('The three bags give 12 counters. Add the 2 loose ones.',bar(14,[12,2],['inside bags','loose']),'3 × 4 + 2 = 14')]};
 if(c==='AREA'&&/missing/.test(o))return {title:'How many squares in each row?',frames:[f('A rectangle covers 24 square units. It has four equal rows.',{kind:'ratio',names:['Rows'],units:[4],unitValue:null,total:24}),f('Share the 24 squares equally between those four rows. There are six in each row.',{kind:'array',rows:4,cols:6},'24 ÷ 4 = 6'),f('Six squares in each row means the missing length is 6 units.',{kind:'scene',scene:'area',values:[6,4],phase:1},'6 × 4 = 24')]};
 if(c==='AREA'&&/composite|rectilinear/.test(o))return {title:'Split the shape into familiar pieces',frames:[f('Imagine the full 6 by 4 rectangle. It would cover 24 square units.',picture({type:'area',a:6,b:4},'The complete rectangle.')),f('A 2 by 2 corner is missing. That missing square covers 4 square units.',picture({type:'area',a:6,b:4,cut:2},'A square corner has been removed.')),f('Only count the area that is still there: 24 minus 4 is 20.',picture({type:'area',a:6,b:4,cut:2},'The remaining L-shape covers twenty square units.'),'6 × 4 − 2 × 2 = 20')]};
 if(c==='VOL'&&/unknown|base area|root/.test(o))return {title:'Recover the missing layers',frames:[f('The cuboid contains 36 cubes. Its base is 4 by 3, so each layer contains 12.',{kind:'geometry',mode:'solid',a:4,b:1,c:3},'4 × 3 = 12 cubes per layer'),f('Two layers contain 24 cubes. We need one more layer to reach 36.',{kind:'geometry',mode:'solid',a:4,b:2,c:3}),f('There are three layers, so the height is 3 units.',{kind:'geometry',mode:'solid',a:4,b:3,c:3},'height = 36 ÷ 12 = 3')]};
 if(c==='VOL'&&/convert/.test(o))return {title:'Two names for the same space',frames:[f('A cube measuring 1 cm on every edge occupies one cubic centimetre.',{kind:'geometry',mode:'solid',a:1,b:1,c:1},'1 cm³'),f('That space holds 1 millilitre of liquid. The two units describe the same volume.',{kind:'geometry',mode:'solid',a:1,b:1,c:1},'1 cm³ = 1 ml'),f('A litre is 1000 millilitres. So one litre occupies 1000 cubic centimetres.',{kind:'geometry',mode:'solid',a:10,b:10,c:10},'1 litre = 1000 ml = 1000 cm³')]};
 if(c==='ANGLE'&&/opposite/.test(o)&&!/straight/.test(o))return {title:'Two ways to finish the same straight turn',frames:[f('The blue angle is 60°. The adjacent orange angle must be 120° to make a straight turn.',{kind:'scene',scene:'angles',values:[60],phase:2},'60° + 120° = 180°'),f('The angle across the crossing shares that same 120° neighbour. It must also be 60°.',{kind:'scene',scene:'crossing',values:[60],phase:1,labels:['Vertically opposite angles both measure sixty degrees. The adjacent angle is one hundred twenty degrees.']},'180° − 120° = 60°'),f('That is why vertically opposite angles match: each completes a straight turn with the same neighbour.',{kind:'scene',scene:'angles',values:[60],phase:2})]};
 if(c==='ANGLE'&&/around/.test(o)&&!/straight/.test(o))return {title:'Turn all the way back to the start',frames:[f('A quarter-turn takes you to a new direction.',picture({type:'angle',degrees:90},'A right-angle turn.'),'90°'),f('Four quarter-turns bring you back to your starting direction.',picture({type:'shape',shape:'circle'},'A complete turn around a point.'),'90° + 90° + 90° + 90° = 360°'),f('If 140° of the full turn is known, the rest must fill the remaining 220°.',bar(360,[140,220],['known turn','remaining turn']),'360° − 140° = 220°')]};
 if(c==='GRAPH'&&/line/.test(o))return {title:'Follow the change over time',frames:[f('The labels below the graph tell us the order of the days.',picture({type:'bars',labels:['Mon','Tue','Wed','Thu'],values:[4,8,6,10],scale:2,line:true},'Values recorded on four consecutive days.')),f('Monday is 4 and Tuesday is 8. The upward line shows an increase.',picture({type:'bars',labels:['Mon','Tue'],values:[4,8],scale:2,line:true},'The value increases from four to eight.'),'8 − 4 = 4'),f('From Tuesday to Wednesday the line goes down. The value decreased by 2.',picture({type:'bars',labels:['Tue','Wed'],values:[8,6],scale:2,line:true},'The value decreases from eight to six.'),'8 − 6 = 2')]};
 if(c==='GRAPH'&&/pie/.test(o))return {title:'The whole circle means everybody',frames:[f('The class has 20 pupils. This entire circle stands for all 20.',picture({type:'pie',labels:['Walk','Bus','Car'],values:[10,5,5]},'Half walk; a quarter use the bus; a quarter come by car.')),f('The walking sector covers half the circle. It represents half the pupils.',strip([2],[1]),'1/2 of 20 = 10'),f('The bus sector covers one quarter. One quarter of the 20 pupils is 5.',strip([4],[1]),'1/4 of 20 = 5')]};
 if(c==='GRAPH'&&/bar/.test(o))return {title:'Read the scale, then the bar',frames:[f('Each step up the scale stands for 2 votes.',picture({type:'bars',labels:['Mango','Apple'],values:[6,4],scale:2},'The scale increases in twos.')),f('The mango bar reaches 6. That means 6 votes, not 3 votes.',picture({type:'bars',labels:['Mango'],values:[6],scale:2},'Three intervals represent six votes.'),'3 × 2 = 6'),f('The apple bar reaches 4. Compare the tops: mango has 2 more votes.',{kind:'bar',whole:null,parts:[],compare:{top:6,bottom:4,names:['Mango','Apple']}},'6 − 4 = 2')]};
 if(c==='LENGTH')return {title:'Begin at zero',frames:[f('Line up one end of the ribbon with zero. Each space on the ruler represents 1 centimetre.',picture({type:'ruler',length:5},'The ribbon starts at zero and ends at five.')),f('Count the spaces between marks: one, two, three, four, five.',picture({type:'ruler',length:5},'Five centimetre intervals span the ribbon.')),f('The ribbon is 5 cm long. The zero mark is a starting point, not an extra centimetre.',picture({type:'ruler',length:5},'The ribbon measures five centimetres.'),'5 − 0 = 5 cm')]};
 return null;
}
